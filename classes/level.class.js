class Level {
    layers;
    chickens;
    clouds;
    
    constructor (layers, chickens, clouds) {
        this.layers = layers;
        this.chickens = chickens;
        this.clouds = clouds;
    }
}